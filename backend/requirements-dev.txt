# Development & test dependencies.
# Installs everything in requirements.txt PLUS the tools needed to run the test
# suite. The production Docker image installs requirements.txt only, so these
# never bloat the shipped image.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.5
anyio==4.13.0
moto==5.0.24
httpx==0.27.2
pytest-cov==6.0.0
