fastapi==0.115.5
uvicorn==0.32.1
boto3==1.35.74
python-dotenv==1.0.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
asyncpg==0.30.0
alembic==1.14.0
pydantic-settings==2.6.1
rich==13.9.4
apscheduler==3.10.4
sendgrid==6.12.5

# --- Auth (Phase 6.1) ---
bcrypt==5.0.0                  # password hashing (used directly, not via passlib)
PyJWT==2.10.1                  # JWT sign/verify (maintained; no datetime.utcnow deprecation)
email-validator==2.2.0         # enables pydantic EmailStr

# --- Encryption (Phase 6.2) ---
cryptography==48.0.0           # Fernet — encrypts users' AWS secrets at rest

# Test/dev-only dependencies live in requirements-dev.txt so the production
# Docker image installs runtime deps only. For local dev + tests run:
#   pip install -r requirements-dev.txt
