# Business logic goes here — kept separate from route handlers.
# Routes handle HTTP (what comes in, what goes out).
# Services handle the actual work (query AWS, run calculations, etc.).
# This separation makes the code easier to test and reuse.
