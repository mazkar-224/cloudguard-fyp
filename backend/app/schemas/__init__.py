# Pydantic schemas go here.
# Schemas define the shape of data coming IN (requests) and going OUT (responses).
# They are separate from SQLAlchemy models on purpose — what you store in the
# database doesn't have to be identical to what you send over the API.
